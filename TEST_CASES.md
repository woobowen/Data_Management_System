# TEST_CASES.md

| 阶段 | 测试步骤 | 输入 | 输出 | 结果 |
| --- | --- | --- | --- | --- |
| 阶段一 | 1. 注册用户 2. 登录获取 JWT 3. 创建问卷草稿 4. 发布问卷 5. 调用 `/api/surveys/:id/render` | 用户名 `alice`、问卷标题 `职业调查`、包含 3 道题目的问卷定义 | 返回 `200`，问卷状态为 `published`，公开渲染数据中不暴露 `ownerId` | 通过 |
| 阶段一 | 1. 登录后台 2. 在编辑器中新增题目 3. 配置题型/选项/跳转 4. 保存草稿 | 单选题 `q1`，选项 `yes/no`，默认跳转 `END` | 问卷详情接口可读到新增题目，结构合法 | 通过 |
| 阶段一 | 1. 创建带分支问卷并发布 2. 按分支路径提交答案 | `q1=optA`，`q2=5` | `/submit` 返回 `201`，后端接受合法路径 | 通过 |
| 阶段一 | 1. 创建并发布数字题问卷 2. 提交越界值 | `q1=optA`，`q2=100` | 返回 `400`，提示数字题高于最大值 | 通过 |
| 阶段一 | 1. 创建并发布带分支问卷 2. 伪造不可达题答案 | `q1=optB`，`q2=3`，`q3=正在求职` | 返回 `400`，拦截幽灵答案 | 通过 |
| 阶段一 | 1. 创建 `allowAnonymous=false` 问卷并发布 2. 匿名提交 | `q1=optA`，`q2=5` | 返回 `401`，提示需登录 | 通过 |
| 阶段一 | 1. 提交合法答卷 2. 调用统计接口 | 问卷 ID | 返回 `optionCounts/average/textValues/responseCount` | 通过 |
| 阶段二 | 1. 创建题库题目 2. 在问卷中复用该题目 3. 检查问卷题元数据 | `questionTemplateId/questionTemplateVersion` | 问卷题保存题库关联元数据 | 通过 |
| 阶段二 | 1. 创建题库题目 2. 编辑题目 | 更新标题与描述 | 生成新版本（新 `_id`），旧版本可读 | 通过 |
| 阶段二 | 1. 打开版本历史 2. 选择历史版本回退 | 目标历史版本 ID | 创建新最新版本；最新版本禁止再次回退 | 通过 |
| 阶段二 | 1. 设置共享用户 2. 共享用户查询题库 | `PUT/GET /api/questions/:id/shares` | 共享用户可见题目；非拥有者不可改共享 | 通过 |
| 阶段二 | 1. 查询题目使用关系 2. 校验引用问卷列表 | `GET /api/questions/:id/usages` | 返回显式引用与历史数据推断引用（`matchMode`） | 通过 |
| 阶段二 | 1. 创建同一题目的多个版本 2. 查询题库列表 | `GET /api/questions` | 主列表仅返回每条版本链的当前最新版本 | 通过 |
| 阶段二 | 1. 创建/更新/回退题目时填写版本备注 2. 查询列表与历史 | `versionRemark` | 主列表与历史均展示版本备注与版本号 | 通过 |
| 阶段二 | 1. 删除某题当前版本 2. 再查题库列表 | `DELETE /api/questions/:id` | 删除整条版本链，不会回退显示旧版本 | 通过 |

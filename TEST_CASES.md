# TEST_CASES.md

| 测试步骤 | 输入 | 输出 | 结果 |
| --- | --- | --- | --- |
| 1. 注册用户 2. 登录获取 JWT 3. 创建问卷草稿 4. 发布问卷 5. 调用 `/api/surveys/:id/render` | 用户名 `alice`、问卷标题 `职业调查`、包含 3 道题目的问卷定义 | 返回 `200`，问卷状态为 `published`，公开渲染数据中不暴露 `ownerId` | 通过 |
| 1. 登录后台 2. 在编辑器中新增题目 3. 填写题目 ID、题型、标题、选项与默认跳转 4. 保存草稿 | 单选题 `q1`，选项 `yes/no`，默认跳转 `END` | 后端成功保存草稿，问卷详情接口可读到新增题目 | 通过 |
| 1. 创建带分支的问卷 2. 发布问卷 3. 选择第一题分支答案 4. 提交该路径对应的后续答案 | `q1=optA`，`q2=5` | `/submit` 返回 `201`，说明动态跳转路径被正确接受 | 通过 |
| 1. 创建并发布数字题问卷 2. 提交超出约束范围的数值答案 | `q1=optA`，`q2=100` | 返回 `400`，错误信息提示数字题超出最大值 | 通过 |
| 1. 创建并发布带分支的问卷 2. 选择应跳到 `q3` 的答案 3. 伪造额外的 `q2` 答案一起提交 | `q1=optB`，`q2=3`，`q3=正在求职` | 返回 `400`，错误信息提示存在幽灵答案或非法跳题 | 通过 |
| 1. 创建 `allowAnonymous=false` 的问卷 2. 发布问卷 3. 不带 JWT 提交答卷 | `q1=optA`，`q2=5` | 返回 `401`，提示该问卷要求登录后填写 | 通过 |
| 1. 提交一份合法答卷 2. 调用 `/api/statistics/surveys/:id` 查询统计结果 | 已提交答卷数据，问卷 ID | 返回各题统计结果：单选题有 `optionCounts`，数字题有 `average`，文本题有 `textValues`，每题有 `responseCount` | 通过 |

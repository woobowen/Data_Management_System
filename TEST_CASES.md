# TEST_CASES.md

## 说明

本报告汇总了两部分测试结果：

- 自动化集成测试：来源于 `tests/integration.test.ts`
- 手工联调测试：来源于阶段一最终联调过程

状态说明：

- `Pass`：测试已执行并通过
- `Fail`：当前未发现失败项

## 一、自动化集成测试

| 测试项 | 测试步骤 | 输入 | 输出 | 结果 |
| --- | --- | --- | --- | --- |
| 创建问卷与发布流程测试 | 1. 注册用户 2. 登录获取 JWT 3. 创建问卷 4. 发布问卷 5. 调用 `/render` | 用户名 `alice`、问卷 `职业调查`、3 道题目定义 | 返回 `200`，`data.status=published`，`questions.length=3`，且 `ownerId` 不暴露 | Pass |
| 提交问卷测试 | 1. 创建并发布问卷 2. 提交合法路径答案 3. 查询统计接口 | `q1=optA`, `q2=5` | `/submit` 返回 `201`；统计中 `q1.optionCounts=[{optA:1}]`，`q2.average=5` | Pass |
| 跳转逻辑测试 | 1. 构造带分支的问卷 2. 选择 `q1=optA` 3. 观察是否只接受 `q2` 路径答案 | `q1=optA`, `q2=5` | 后端接受该路径，证明动态跳转生效 | Pass |
| 校验测试：数值越界 | 1. 创建并发布问卷 2. 对数字题提交超出上限的值 | `q1=optA`, `q2=100` | 返回 `400`，错误消息包含“数字题高于最大值” | Pass |
| 校验测试：幽灵答案 / 非法跳题 | 1. 创建并发布问卷 2. 选择 `q1=optB`，理论路径应到 `q3` 3. 伪造多余 `q2` 答案一起提交 | `q1=optB`, `q2=3`, `q3=正在求职` | 返回 `400`，错误消息包含“幽灵答案” | Pass |
| 校验测试：匿名权限 | 1. 创建 `allowAnonymous=false` 问卷 2. 不携带 JWT 提交 | `q1=optA`, `q2=5` | 返回 `401`，错误消息包含“登录” | Pass |
| 统计测试 | 1. 提交一份合法答卷 2. 请求 `/api/statistics/surveys/:id` | 已提交答卷数据 | 单选题得到 `optionCounts`，数字题得到 `average`，每题都有 `responseCount` | Pass |

## 二、手工全链路联调测试

### 1. 真实 `/render` 联调

- 测试步骤：
  1. 启动后端 `3000` 端口与前端 `5173` 端口
  2. 创建并发布真实测试问卷
  3. 浏览器访问 `/?surveyId=<真实ID>`
- 输入：
  - 真实问卷 ID：`69c6aa1e3ccafd920f62bdf0`
- 输出：
  - 页面成功渲染标题、题目、页码和选项浮雕控件
- 结果：
  - Pass

### 2. 前端跳转与翻页联调

- 测试步骤：
  1. 第一题点击“外勤回访”
  2. 点击“下一页”
  3. 观察是否翻到数字题 `q2`
- 输入：
  - `q1=field`
- 输出：
  - 页面从 `q1` 正确切换到 `q2`
  - 页码从 `第 1 页 / 共 2 页` 增加到 `第 2 页 / 共 2 页`
- 结果：
  - Pass

### 3. 合法答卷提交流程

- 测试步骤：
  1. 第一题选择 `外勤回访`
  2. 第二题输入 `3`
  3. 第三题多选中点击 `归档盖章`
  4. 点击“确认印章 / 提交”
- 输入：
  - `q1=field`
  - `q2=3`
  - `q4=['seal']`
- 输出：
  - 前端进入封卷页
  - 无报错提示
- 结果：
  - Pass

### 4. 数据落库验证

- 测试步骤：
  1. 提交真实答卷
  2. 调用 `/api/statistics/surveys/69c6aa1e3ccafd920f62bdf0`
- 输入：
  - 问卷 ID：`69c6aa1e3ccafd920f62bdf0`
- 输出：
  - `q1.optionCounts` 中 `field=1`
  - `q2.average=3`
  - `q4.optionCounts` 中 `seal=1`
  - `q3.responseCount=0`
- 结果：
  - Pass

### 5. 浏览器端稳定性检查

- 测试步骤：
  1. 打开前端页面
  2. 查看控制台
  3. 检查是否存在 hydration、key、动画异常
- 输入：
  - 正常访问前端页面
- 输出：
  - 无 hydration 报错
  - 无 key 缺失报错
  - 仅存在 React DevTools 提示信息
- 结果：
  - Pass

## 三、覆盖性总结

本阶段测试已经覆盖以下关键能力：

- 注册 / 登录
- 创建问卷 / 发布问卷
- 获取公开问卷定义
- 动态跳转路径提交
- 非法输入拦截
- 幽灵答案拦截
- 匿名权限控制
- 统计聚合正确性
- 前后端真实联调与落库验证
